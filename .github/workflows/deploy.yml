name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment: production

    steps:
    # ----------------------
    # Step 1: Checkout Code
    # ----------------------
    - name: 🛎️ Checkout code
      uses: actions/checkout@v4

    # ----------------------
    # Step 2: Setup Node.js
    # ----------------------
    - name: 🧱 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20

    # ----------------------
    # Step 3: Install & Build Angular App
    # ----------------------
    - name: 🔧 Install dependencies
      run: npm ci

    - name: 🏗️ Build Angular app
      run: npm run build --prod

    # ----------------------
    # Step 4: Run Angular Tests
    # ----------------------
    - name: ✅ Run unit tests
      run: npm run test -- --watch=false --browsers=ChromeHeadless

    # ----------------------
    # Step 5: Setup SSH
    # ----------------------
    - name: 🔐 Setup SSH
      uses: webfactory/ssh-agent@v0.9.0
      with:
        ssh-private-key: ${{ secrets.ORACLE_VM_SSH_KEY }}

    # ----------------------
    # Step 6: Add Server to Known Hosts
    # ----------------------
    - name: 🤝 Add server to known hosts
      run: |
        SSH_HOST="${{ secrets.ORACLE_VM_HOST }}"
        echo "🔍 Verifying SSH host..."
        if [ -z "$SSH_HOST" ]; then
          echo "❌ SSH_HOST is no_
